#include <stdio.h> // remove this header file, program still run but gives a warning
// to either explicitly declare printf or include this header as this contains I/O functions

main(){ // exclude int, will give warning again on no type specifier mentioned, defaults to int, but program still runs 

	printf("%s","n" );
}